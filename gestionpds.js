/* gestionpds.js */
function getDebutSemaine(date){const d=new Date(date);const jour=d.getDay();const decalage=(jour===6)?0:(jour+1);d.setDate(d.getDate()-decalage);d.setHours(0,0,0,0);return d;}function getFinSemaine(samedi){const d=new Date(samedi);d.setDate(d.getDate()+6);d.setHours(23,59,59,999);return d;}function getBornesSemaineEnCours(){const maintenant=new Date();let samedi;if(maintenant.getDay()===0){samedi=new Date(maintenant);samedi.setDate(maintenant.getDate()+6);samedi.setHours(0,0,0,0);}else{samedi=getDebutSemaine(maintenant);}return{debut:samedi,fin:getFinSemaine(samedi)};}function getBornesSemainePrecedente(){const hier=new Date();hier.setDate(hier.getDate()-1);const samedi=getDebutSemaine(hier);return{debut:samedi,fin:getFinSemaine(samedi)};}function toYMD(date){return date.toISOString().split('T')[0];}function formatHeures(heures){const h=Math.floor(heures);const min=Math.round((heures-h)*60);if(h===0&&min===0)return '0h 0min';return `${h}h ${min}min`;}function labelSemaine(debut){const fin=new Date(debut);fin.setDate(debut.getDate()+6);const opts={day:'2-digit',month:'long'};const optsA={day:'2-digit',month:'long',year:'numeric'};return `Sam. ${debut.toLocaleDateString('fr-FR', opts)} → Ven. ${fin.toLocaleDateString('fr-FR', optsA)}`;}async function fetchHistorique(debut,fin){const{data,error}=await window.supabaseClient.from('pds_historique').select('*').gte('date',toYMD(debut)).lte('date',toYMD(fin)).order('date',{ascending:true});if(error){console.error('fetchHistorique :',error);return[];}return data||[];}async function fetchServicesEnCours(){const{data,error}=await window.supabaseClient.from('pds_en_cours').select('user_id').eq('termine',false);if(error){console.error('fetchServicesEnCours :',error);return[];}return data||[];}async function fetchArchives(){const{data,error}=await window.supabaseClient.from('pds_archives').select('*').order('week_key',{ascending:false});if(error){console.error('fetchArchives :',error);return[];}return data||[];}async function archiverSiDimanche(){const maintenant=new Date();if(maintenant.getDay()!==0)return;if(maintenant.getHours()===0&&maintenant.getMinutes()<1)return;const{debut,fin}=getBornesSemainePrecedente();const weekKey=toYMD(debut);const{data:existante}=await window.supabaseClient.from('pds_archives').select('id').eq('week_key',weekKey).maybeSingle();if(existante){console.log(`Archive ${weekKey} déjà présente en base.`);return;}const historique=await fetchHistorique(debut,fin);const joueurs=regrouperJoueurs(historique,[]);const snapshot=joueurs.map(j=>({user_id:j.user_id,email:j.email,total:j.total,jours:j.jours}));const{error}=await window.supabaseClient.from('pds_archives').upsert({week_key:weekKey,week_label:labelSemaine(debut),data:snapshot,archived_at:new Date().toISOString()},{onConflict:'week_key'});if(error){console.error('Erreur archivage :',error);}else{console.log(`✅ Archive Supabase créée : ${weekKey}`);}}function regrouperJoueurs(historique,servicesActifs){const actifsIds=new Set(servicesActifs.map(s=>s.user_id));const map={};historique.forEach(entry=>{if(!map[entry.user_id]){map[entry.user_id]={user_id:entry.user_id,email:entry.email,total:0,jours:[],enService:actifsIds.has(entry.user_id)};}const h=parseFloat(entry.heures_travaillees)||0;map[entry.user_id].total+=h;map[entry.user_id].jours.push({date:entry.date,jour:entry.jour_semaine,heures:h});});return Object.values(map).filter(j=>j.total>0).sort((a,b)=>b.total-a.total);}function renderCarteJoueur(joueur){const joursHTML=joueur.jours.sort((a,b)=>a.date.localeCompare(b.date)).map(j=>`
            <div class="pds-session-item">
                <span class="session-jour">${j.jour}</span>
                <span class="session-date">${new Date(j.date + 'T12:00:00').toLocaleDateString('fr-FR')}</span>
                <span class="session-duree">${formatHeures(j.heures)}</span>
            </div>
        `).join('');const badgeService=joueur.enService?`<span class="badge-en-service">🔴 En service</span>`:'';return `
        <div class="pds-card">
            <div class="pds-player-header">
                <div class="pds-player-name">👤 ${joueur.email}</div>
                ${badgeService}
            </div>
            <div class="pds-total">⏱ Total semaine : ${formatHeures(joueur.total)}</div>
            <div class="pds-label">Détail par jour :</div>
            <div class="pds-sessions">
                ${joursHTML || '<span class="pds-empty">Aucune entrée</span>'}
            </div>
        </div>
    `;}async function afficherSemaineEnCours(){const container=document.getElementById('semaine-en-cours');const counter=document.getElementById('count-semaine-cours');const{debut,fin}=getBornesSemaineEnCours();const[historique,servicesActifs]=await Promise.all([fetchHistorique(debut,fin),fetchServicesEnCours()]);const joueurs=regrouperJoueurs(historique,servicesActifs);counter.textContent=joueurs.length;if(joueurs.length===0){container.innerHTML=`
            <div class="no-data">
                Aucune prise de service pour la semaine en cours<br>
                <small>(${labelSemaine(debut)})</small>
            </div>`;return;}container.innerHTML=joueurs.map(renderCarteJoueur).join('');}async function afficherSemainesPrecedentes(){const container=document.getElementById('semaines-precedentes');const counter=document.getElementById('count-semaines-precedentes');const archives=await fetchArchives();counter.textContent=archives.length;if(archives.length===0){container.innerHTML=`<div class="no-data">Aucune semaine archivée pour le moment.</div>`;return;}container.innerHTML=archives.map(archive=>{const nb=Array.isArray(archive.data)?archive.data.filter(j=>j.total>0).length:0;return `
            <div class="semaine-card" onclick="ouvrirModal('${archive.week_key}')">
                <div class="semaine-label">📅 ${archive.week_label}</div>
                <div class="semaine-sub">${nb} joueur(s) actif(s) cette semaine-là</div>
            </div>
        `;}).join('');}let cacheArchives=null;async function ouvrirModal(weekKey){const overlay=document.getElementById('modal-semaine');const titre=document.getElementById('modal-titre');const content=document.getElementById('modal-content');overlay.classList.add('active');content.innerHTML=`<div class="loading">Chargement…</div>`;if(!cacheArchives){cacheArchives=await fetchArchives();}const archive=cacheArchives.find(a=>a.week_key===weekKey);if(!archive){content.innerHTML=`<div class="no-data">Archive introuvable.</div>`;return;}titre.textContent=`📋 ${archive.week_label}`;const joueurs=(archive.data||[]).filter(j=>j.total>0);content.innerHTML=joueurs.length===0?`<div class="no-data">Aucun joueur actif cette semaine-là.</div>`:joueurs.map(j=>renderCarteJoueur({user_id:j.user_id,email:j.email,total:j.total,jours:j.jours||[],enService:false})).join('');}function fermerModal(){document.getElementById('modal-semaine').classList.remove('active');}document.addEventListener('DOMContentLoaded',async()=>{await archiverSiDimanche();await Promise.all([afficherSemaineEnCours(),afficherSemainesPrecedentes()]);document.getElementById('modal-semaine')?.addEventListener('click',function(e){if(e.target===this)fermerModal();});setInterval(async()=>{cacheArchives=null;await afficherSemaineEnCours();},60_000);});
